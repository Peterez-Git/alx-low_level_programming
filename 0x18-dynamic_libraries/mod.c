#include <stdio.h>

/**
 * mod - The function finds the remainder the first number divided by the second 
 * @a: First number variable
 * @b: Second number variable
 * Return: returns the modulus of both numbers
 */
int mod(int a, int b)
{
	return (a % b);
}
