#include <stdio.h>

/**
 * add - The function adds two numbers together
 * @a: First number variable
 * @b: Second number variable
 * Return: addition of both numbers
 */
int add(int a, int b)
{
	return (a + b);
}
